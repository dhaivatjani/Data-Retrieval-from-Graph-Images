# Sample Data

This folder contains a small number of representative graph examples.

Included graph types:

- `bar/`
- `line/`
- `scatter/`

Each numbered folder corresponds to one sample graph image from the original testing data. Some folders also contain the extracted CSV output for that graph.

Only three samples per graph type are included to keep the GitHub repository clean and lightweight.
